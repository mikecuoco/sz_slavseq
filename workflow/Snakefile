from snakemake.utils import min_version

##### set minimum snakemake version #####
min_version("7.8.5")
# Main entrypoint of the workflow. 
# Please follow the best practices: 
# https://snakemake.readthedocs.io/en/stable/snakefiles/best_practices.html,
# in particular regarding the standardized folder structure mentioned there. 

configfile: "config/config.yml"

extensions = [".fa", ".fa.amb", ".fa.ann", ".fa.bwt", ".fa.fai", ".fa.pac", ".fa.sa", ".genome"]

include: "rules/common.smk"
include: "rules/ref.smk"
include: "rules/trim.smk"
include: "rules/align.smk"
include: "rules/model.smk"
include: "rules/test.smk"


rule all:
    input:
        expand("results/flank_features/{donor}/{type}/{sample}.pickle.gz", donor=samples['donor_id'], type=samples['sample_type'], sample=samples['sample_id'])
