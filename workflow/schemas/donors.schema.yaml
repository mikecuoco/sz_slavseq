$schema: "https://json-schema.org/draft/2020-12/schema"

description: an entry in the tissue sheet

properties:
  donor_id:
    type: string
    description: donor identifier
  libd_id:
    type: string
    description: libd identifier

required:
  - donor_id
  - libd_id
